# Enable/Disable prmiscuous mode

Enable promiscuous mode on eth1 network interface :  
```
ip link set eth1 promisc on
```
Disable promiscuous mode on eth1 network interface :  
```
ip link set eth1 promisc off
```